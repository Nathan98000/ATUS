# Base URL of the Phase 3 analytical API (no trailing slash). The frontend
# appends /api/v1 itself. The API must allow this frontend's origin via
# ATUS_API_CORS_ORIGINS (see the repository root .env.example).
VITE_API_BASE_URL=http://localhost:8000
